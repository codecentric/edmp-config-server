## edmp-config-server Docker Container

Usage:

```
cd edmp-config-server
docker build -t edmp-config-server .
```

## Run

```shell
docker run --name="edmp-config-server" -p 18888:8888 edmp-config-server
```
